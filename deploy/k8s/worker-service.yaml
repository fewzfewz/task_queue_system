apiVersion: v1
kind: Service
metadata:
  name: task-queue-worker
  namespace: task-queue
  labels:
    app: task-queue-worker
    app.kubernetes.io/part-of: task-queue
spec:
  selector:
    app: task-queue-worker
  ports:
    - port: 8081
      targetPort: 8081
      name: http
  type: ClusterIP
