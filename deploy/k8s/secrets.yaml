apiVersion: v1
kind: Secret
metadata:
  name: task-queue-secrets
  namespace: task-queue
type: Opaque
stringData:
  # Override these values before applying to production:
  api-key: "secret-api-key"
  admin-password: "admin123"
  postgres-conn-str: ""
